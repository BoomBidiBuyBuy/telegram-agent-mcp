{
    "mcpServers": {
        "ReplyService": {
            "transport": "streamable_http",
            "url": "http://${REPLY_SERVICE_MCP_HOST}:${REPLY_SERVICE_MCP_PORT}/mcp/"
        },
        "DatabaseService": {
            "transport": "streamable_http",
            "url": "http://${DATABASE_SERVICE_MCP_HOST}:${DATABASE_SERVICE_MCP_PORT}/mcp/"
        }
    }
}